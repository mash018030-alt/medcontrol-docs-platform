# Тёмно-синий (референс)

HEX: **`#1A3399`**

Используется для заголовков статей в документации, текста и иконок на плитках заглавных страниц (`--docs-temno-sinii` в `App.css`), а также для акцентов (например, цвет подчёркивания ссылок при наведении в навигации).
