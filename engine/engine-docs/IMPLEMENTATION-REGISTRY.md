# Реестр реализации движка (живой документ)

**Назначение:** кратко фиксировать *как сейчас устроены* нетривиальные части `engine`, чтобы при следующих правках не «ломать неочевидное». Документ **обновляется вместе с кодом** (в т.ч. после вашего ОС на правки).

**Связанные регламенты** (углубленно): см. таблицу в [`README.md`](README.md).

| Область | Ключевые файлы / пути | Инварианты (не ломать без осознанного ТЗ) | Обновлено |
|--------|------------------------|--------------------------------------------|-----------|
| **Версии документации (переключатель, query, архив .md)** | `src/components/DocsVersionSwitcher.jsx`, `src/styles/docsVersionSwitcher.css`, `src/data/docsDocumentationVersions.js`, `src/utils/docsVersionNav.js`, `src/data/docsArticleVersioning.js`, `src/components/Article.jsx` | Query-параметр из `DOCS_VERSION_QUERY_KEY` (`docVer`); неактуальная редакция → `fetch` текста из `buildArticleMarkdownUrl` (**не** из актуального `content/`). Внутренние ссылки при архиве дополняются через `appendDocVerToInternalHref`. **Архив:** только пути из allowlist в `docsArticleVersioning.js`. **Блок «История версий»** внизу статьи (`getArticleVersionHistory`): по умолчанию строится из `ARTICLE_PATHS_BY_SNAPSHOT_VERSION` + строка актуальной линии; у статьи **без** снимков в `previous_versions` блока нет — это ожидаемо. Ручной `DOCS_ARTICLE_VERSION_HISTORY_BY_SLUG` — только для `note`/нестандарта. | 2026-03-30 |
| **Каталог архивных снимков (файлы на диске)** | `public/previous_versions/{cloud\|mobile}/<versionId>/0_docs/…/articles/*.md` и `…/images/<slug статьи>/` (опционально `…/sources/<slug>/` для pdf/zip); актуальный контент — корневой каталог **`content/`** (для Vite — связь `public/content` → `../content`) | URL в коде: `PREVIOUS_VERSIONS_PRODUCT` + `buildArticleMarkdownUrl` → сегмент **`cloud`** (или позже **mobile**). Структура зеркалит `content`: разделы → `articles` + `images/<папка как у статьи>`. | 2026-04-09 |
| **Проверка связки контент ↔ nav (CI)** | `scripts/check-internal-links.mjs`, `.github/workflows/ci.yml` | Для каждого `path` из **`flatArticles`** в `nav.js` должен существовать **`content/<path>.md`**. Плюс внутренние ссылки по markdown, якоря по заголовкам, пути новостей по `news_tree.json`. Предупреждение «md есть, но нет в flatArticles» не роняет скрипт. Переменная **`LINK_CHECK_STRICT=0`** — выход 0 при ошибках (только локальный отчёт). | 2026-04-09 |
| **Rehype / react-markdown pipeline** | `src/docsMarkdownRehypePlugins.js`, `src/rehype-*.js` | Плагины в `use([...])` передавать как **attacher-функции без преждевременного `()`** (иначе unified трактует inner `(tree)=>…` как attacher и вызывает без AST → `'children' in undefined`). Порядок: `rehypeRaw` → `rehypeFootnotesSection` → `rehypePublicAssets` → `rehypeInlineTooltip` → `rehypeFootnoteTooltips` → `rehypeSanitizeChildLists`. | 2026-03-30 |
| **Макрос «архивная редакция» в статьях снимка** | Архивные `.md` в `previous_versions/.../articles/` | Для предупреждения «вы смотрите старую версию» в теле снимка использовать **`docs-attention` / `docs-attention-title`** (не `docs-info`). Справочные блоки — по смыслу `docs-info`, `docs-note` и т.д. | 2026-04-08 |
| **Таблицы в статьях** | `src/components/MarkdownTable.jsx`, `MarkdownTr.jsx`, `App.css` | См. **`TABLES-DISPLAY.md`**. | — |
| **PDF** | `pdf-server/`, `runArticlePdfExport.js`, прокси в `vite.config.js` | См. **`PDF-EXPORT.md`**. | — |
| **Маршруты: главная платформы vs дашборд** | `src/constants/docsRoutes.js` (`DOCS_DASHBOARD_PATH`), `App.jsx`, `src/components/platform/PlatformHomePage.jsx`, `Layout.jsx`, `Header.jsx`, `Sidebar.jsx`, `docsBackNavigation.js`, `Article.jsx`, `SectionPdfBundlePage.jsx` | **`/`** — разводящая «платформа» (класс `docs-layout--platform-home`, блок `.docs-platform-home`); **без** кнопок «документация / новости» в теле страницы — переход к продуктам через переключатель в шапке. Дашборд документации текущего контента — **`/documentation`** (`DOCS_DASHBOARD_PATH`). Лого продукта на **`/`** скрыто; ссылка лого и «главная документации» — на **`/documentation`**. Путь дашборда не должен пересекаться со slug статей из `nav.js`. | 2026-04-09 |
| **Угловая метка «НИИ_Docs»** | `src/components/NiiDocsCornerMark.jsx`, `Layout.jsx`, `App.css` (`.docs-nii-corner-mark`) | Фиксированная ссылка **`/`** (тот же origin, что у страницы) в правом нижнем углу на всех маршрутах под `Layout`. **Скрыта** при `mc_pdf=1` и в **`@media print`**. Стили — нейтральные (серые тона), без привязки к бренду продукта в `theme.css`. | 2026-04-09 |
| **Тема / бренд (контент)** | `content/repo_docs/brand/theme.css`, `content/repo_docs/brand/README.md`; `attachContentBrandTheme.js`, `resolveDocsLogoUrl.js`, `resolveDocsHomeAriaLabel.js`; `main.jsx`, `Header.jsx`; **`vite.config.js`** (`html-docs-head` → `<title>`, `<link rel="icon">`) | Нейтральные токены в **`App.css`** (`:root`); продукт — **`theme.css`** по URL **`/content/repo_docs/brand/theme.css`** (отключить: **`VITE_DOCS_USE_CONTENT_THEME=0`**). Лого: **`VITE_DOCS_LOGO_URL`**. Заголовок: **`VITE_DOCS_SITE_TITLE`**. Favicon: **`VITE_DOCS_FAVICON_URL`** / **`VITE_DOCS_FAVICON_TYPE`**. Подпись лого: **`VITE_DOCS_HOME_ARIA_LABEL`**. Вся ветка **`content/repo_docs/`** не обходится как статьи в `check-internal-links.mjs` и `fix-md-internal-path-hyphens.mjs` (в т.ч. **`brand/`**). | 2026-04-09 |

## Как вести строку реестра

1. Добавьте/измените строку в таблице или уточните «Инварианты».
2. Колонку **Обновлено** — дата правки (YYYY-MM-DD).
3. Если тема большая — вынесите детали в отдельный `engine-docs/TOPIC.md` и в таблице оставьте одну ссылку.

## История существенных правок (кратко)

- **2026-04-07:** служебные тексты **`content/repo_docs/REFERENCES.md`** (описание каталога **`content/references/`**) и **`content/repo_docs/OBSHEE_CONTENT_SNAPSHOT.md`** вынесены из **`content/references/`**; в **`content/references/README.md`** остался краткий указатель на **`repo_docs/`**.
- **2026-03-30:** «История версий» статьи считается из allowlist снимков + актуальная линия (`getArticleVersionHistory`), без дублирования slug вручную; на публичном сайте блок есть только у статей, у которых реально заведён архивный снимок в `previous_versions/`.
- **2026-03-30:** сноски → тултипы: для `MarkdownInlineTip` hover по сноске не отключается при `(hover: none)` на гибридах Windows (иначе на GH Pages «нет тултипов» при мыши); запасной контейнер для расчёта fixed-панели — `.docs-content` / `main` / `body`; CSS-fallback, пока нет класса `--footnote-float`.
- **2026-03-30:** в `docsMarkdownRehypePlugins.js` снова убраны `()` у кастомных rehype-плагинов (регрессия: падение рендера статей с `'children' in undefined`).
- **2026-04-08:** заведён реестр; зафиксированы версии документации, `previous_versions` (cloud/mobile, статьи+картинки), rehype attacher, макрос предупреждения в архивных статьях.
- **2026-04-09:** контент документации встроен в репозиторий платформы: без submodule; удалён `.gitmodules`; CI/Pages — обычный `checkout` без `submodules: true`.
- **2026-04-09:** пользовательский контент вынесен в корневой каталог **`content/`**; для Vite **`engine/scripts/ensure-content-link.cjs`** (postinstall / predev / prebuild) создаёт **`engine/public/content`** → **`../content`**; **`engine/.gitignore`** — не коммитить саму связь.
- **2026-04-09:** корневой каталог движка переименован **`docs-app/`** → **`engine/`**; **`docs-brand/`** перенесён в **`engine/docs-brand/`**; CI, Dependabot, корневые README и скрипты обновлены под новые пути.
- **2026-04-09:** в корне репозитория остаются **`content/`** и **`engine/`** как основные каталоги; **`archive/`** перенесён в **`engine/archive/`**; **`brief.md`**, **`CONTEXT.md`**, **`REPO-LAYOUT.md`**, **`CURSOR-AGENT-SETTINGS.md`** — в **`engine/repo-docs/`** (индекс **`engine/repo-docs/README.md`**); корневой **`README.md`** — краткая точка входа и пояснение, почему **`.github/`**, **`.cursor/`**, **`.vscode/`**, **`.gitignore`** остаются в корне.
- **2026-04-09:** `check-internal-links.mjs` — проверка **`flatArticles` → файлы в `content/`**; в CI шаг без `continue-on-error` (после успешной `npm run build`).
- **2026-04-09:** шаблон README для архивации отдельного репо контента — **`engine/repo-docs/medcontrol-docs-content-ARCHIVE-README.md`** (копировать в [medcontrol-docs-content](https://github.com/mash018030-alt/medcontrol-docs-content), затем **Archive** в настройках GitHub).
- **2026-04-09:** удалены превью разводящих **`content/images/dashboards/Obshee/`** и **`…/admin/`**; удалены неиспользуемые **`obsheeLandingCardPreview.js`**, **`adminLandingCardPreview.js`** и скрипты **`obshee-9-normalize-width.cjs`**, **`rebuild-obshee-9-preview.cjs`**; из **`engine/package.json`** убраны соответствующие npm-скрипты; регламенты **`OBSHEE-LANDING-SNAPSHOT.md`**, **`OBSHEE_CONTENT_SNAPSHOT.md`**, README и **`.cursor/rules`** обновлены под текущий список ссылок без карточного UI.
- **2026-04-09:** тема продукта вынесена в **`content/repo_docs/brand/theme.css`** (шрифты + CSS‑переменные `:root`); движок подключает её через **`attachContentBrandTheme`**; в **`App.css`** — нейтральные дефолты платформы.
- **2026-04-09:** токены макросов «Примечание»/«Внимание» и тень баннера новостей — в **`theme.css`** / нейтральные в **`App.css`**; **`VITE_DOCS_LOGO_URL`** для логотипа шапки (`resolveDocsLogoUrl`).
- **2026-04-09:** дашборд, карточки поиска, ховеры шапки/баннера, «Важно»/«Информация» — набор **`--docs-*`** в **`App.css`** / MedControl в **`theme.css`**; фокус/подсветка якоря — `color-mix` от **`--docs-ui-accent`**; **`VITE_DOCS_SITE_TITLE`** в **`vite.config.js`** для `<title>`.
- **2026-04-09:** тень карточки в баннере новостей — **`--docs-news-banner-card-shadow`**; favicon и **`aria-label`** лого — **`VITE_DOCS_FAVICON_*`**, **`VITE_DOCS_HOME_ARIA_LABEL`**.
- **2026-04-09:** на **`/`** и **`/safe-mobile`** в шапке — переключатель продуктов **Safe Mobile | MedControl** (нейтральные серые вкладки, класс **`docs-layout--platform-home`**); иначе — **Документация | Новости**. Константа **`SAFE_MOBILE_PATH`**, страница-заглушка **`SafeMobilePlaceholderPage.jsx`**.
- **2026-04-09:** корень **`/`** — главная платформы (`PlatformHomePage`); дашборд MedControl — **`/documentation`** (`DOCS_DASHBOARD_PATH`); навигация и «назад из раздела» синхронизированы с константой.
- **2026-04-09:** с **`PlatformHomePage`** убраны CTA-кнопки «документация / новости»; вход — через переключатель продуктов в шапке.
- **2026-04-09:** **`NiiDocsCornerMark`** — метка **НИИ_Docs** в правом нижнем углу, ссылка на **`/`**; не в PDF-режиме и не при печати.
- **2026-04-09:** текстовый референс палитры перенесён из **`engine/docs-brand/`** в **`content/references/docs-brand/`** (нейтральная платформа — только **`App.css`** в движке; MedControl — **`content/repo_docs/brand/theme.css`** + markdown в контенте); обновлены **`.cursor/rules`**, **`REPO-LAYOUT.md`**, **`brief`**, **`CONTEXT`**, **`TABLES-DISPLAY.md`**, корневой **`README.md`**, **`references/README.md`**.
- **2026-04-09:** **`content/references/Colors/`** убрана как отдельная ветка: дублирующие `.md` удалены, PDF/PNG перенесены в **`content/references/docs-brand/colors/`** (`light 0.png` → **`light-0.png`**); единый канон текста и вложений — **`docs-brand/colors/`**, тема в браузере — **`content/repo_docs/brand/theme.css`**.
- **2026-04-09:** папка темы перенесена с **`content/brand/`** на **`content/repo_docs/brand/`** (меньше верхнеуровневых каталогов под **`content/`**); **`attachContentBrandTheme`** подключает **`/content/repo_docs/brand/theme.css`**.
