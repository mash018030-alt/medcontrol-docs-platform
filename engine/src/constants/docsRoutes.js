/**
 * Дашборд продуктовой документации (корень дерева статей в этом репозитории).
 * Корень сайта `/` — отдельная главная «платформа документации».
 */
export const DOCS_DASHBOARD_PATH = '/documentation'

/** Заглушка второго продукта на платформе (переключатель в шапке на `/` и здесь). */
export const SAFE_MOBILE_PATH = '/safe-mobile'
